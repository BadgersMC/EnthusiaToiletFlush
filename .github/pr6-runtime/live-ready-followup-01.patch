diff --git a/paper-companion/src/test/java/com/badgersmc/queuerestart/paper/RestartExecutorTest.kt b/paper-companion/src/test/java/com/badgersmc/queuerestart/paper/RestartExecutorTest.kt
index 77ca5de..afe1b77 100644
--- a/paper-companion/src/test/java/com/badgersmc/queuerestart/paper/RestartExecutorTest.kt
+++ b/paper-companion/src/test/java/com/badgersmc/queuerestart/paper/RestartExecutorTest.kt
@@ -22,7 +22,11 @@ class RestartExecutorTest {
             queued += run
             return object : ScheduledHandle { override fun cancel() { run.cancelled = true } }
         }
-        fun fireAll() = queued.toList().also(queued::clear).filterNot(Run::cancelled).forEach { it.action() }
+        fun fireAll() {
+            val pending = queued.toList()
+            queued.clear()
+            pending.filterNot(Run::cancelled).forEach { it.action() }
+        }
     }
 
     @Test
